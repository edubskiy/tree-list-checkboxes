import styled from 'styled-components';

export const CheckboxList = styled.ul`
  padding: 0;
  margin: 0;
  list-style-type: none;
`;

export const CheckboxListItem = styled.li`
  display: flex;
  align-items: center;
  padding: 3px;
  color: #444;
  font-size: 12px;
  line-height: 24px;
  cursor: pointer;
`;

export const CheckboxListItemText = styled.div`
  color: #fff;
  opacity: 0.9;
`;
