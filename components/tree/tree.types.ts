import {
  CheckboxItemState,
  CheckboxListItem,
} from '../checkbox-list/checkbox-list.types';

export interface TreeProps {
  checkboxState: CheckboxItemState[];
  checkboxItems: CheckboxListItem[];
}
